# Example .htpasswd file
# Create your actual .htpasswd file with:
#   htpasswd -c .htpasswd yourusername
#
# To add more users:
#   htpasswd .htpasswd anotherusername
#
# IMPORTANT:
# - Copy this to .htpasswd and add your credentials
# - .htpasswd is gitignored to keep passwords secure
# - You need apache2-utils installed: brew install httpd (macOS) or apt-get install apache2-utils (Linux)
